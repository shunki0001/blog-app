<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>BLOG</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="/send_url.js"></script>
</head>
<body>
    <%- include('header') %>
    <main>
        <div class="article">
            <% if (article.category === 'all') { %>
                <h1><%= article.title %></h1>
                <p><%= article.content %></p>
            <% } %>
            <% if (article.category === 'limited') { %>
                <i>会員限定</i>
                <h1><%= article.title %></h1>

                <% if (locals.isLoggedIn) { %>
                    <p><%= article.content %></p>
                <% } else { %>
                    <div class="article-login">
                        <p>今すぐログインしよう！</p>
                        <p>記事の続きは<br>ログインすると読むことができます</p>
                        <img src="/images/login.svg">
                        <a class="btn" href="/login">ログイン</a>
                    </div>
                <% } %>
            <% } %>
        </div>
    </main>
    <footer>
        <a class="btn sub" href="/list">一覧に戻る</a>
    </footer>
</body>
</html>